#ifndef PRIORITY_QUEUE_HPP
#define PRIORITY_QUEUE_HPP

#include <queue>

std::priority_queue<int> generatePQueue(int size); 
int searchMinSTL(std::priority_queue<int> value); 

void runPriorityQueue();

#endif